#include "process_wpp.hpp"
namespace xlang
{
	vlAstAxis * WppProcess(vlContext * context,vlApply* pApply, 
		vlParser* parser,vlAstAxis * anode,std::vector<vlAstAxis *> &args,std::map<int,vlAstAxis*> & paramBind)
	{
		
		return 0;
	}
}
